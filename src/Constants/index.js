import {
  horizontalScale,
  verticalScale,
  moderateScale,
  WIDTH,
  HEIGHT,
} from './Metrics';
import {Fonts} from './Contants';

export {horizontalScale, verticalScale, moderateScale, WIDTH, HEIGHT, Fonts};
